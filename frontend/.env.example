# Copy this file to .env for local overrides if needed.
# In local dev you usually don't need this — Vite proxies /api to Flask automatically.
# In production (Vercel), set this as an Environment Variable in the project settings:
#   VITE_API_URL=https://your-backend.onrender.com/api

VITE_API_URL=
